{
  "name": "@type-dot-com/type-openclaw-plugin",
  "version": "0.1.5",
  "description": "OpenClaw channel plugin for Type — the agent-native team chat platform",
  "type": "module",
  "main": "src/index.ts",
  "openclaw": {
    "extensions": [
      "./src/index.ts"
    ],
    "channel": {
      "id": "type",
      "label": "Type",
      "selectionLabel": "Type (Team Chat)",
      "docsPath": "/channels/type",
      "blurb": "Connect OpenClaw to Type team chat via WebSocket."
    }
  },
  "files": [
    "src/**/*.ts",
    "!src/**/*.test.ts",
    "openclaw.plugin.json",
    "SKILL.md"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "ws": "^8.17.1",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "@types/ws": "^8.5.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
